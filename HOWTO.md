## Karabas-MegaBuzz How-to

### Прозвонка спартана

Запаивание компонентов на плату желательно начать со спартана. 
После запаивания спартана, желательно прозвонить плату.
Для этого мультиметр в режиме прозвонки (диодов), красным щупом становимся на землю. 
А черным щупом тыкаем в пятаки, отмеченные красным цветом на карте прозвонки [верх revA](docs/karabas-megabuzz_revA_top_map.png) и [низ revA](docs/karabas-megabuzz_revA_bottom_map.png).
Для ревизии revA2 также имеются карты прозвонки: [верх revA2](docs/karabas-megabuzz_revA2_top_map.png) и [низ revA2](docs/karabas-megabuzz_revA2_bottom_map.png)
Все отмеченные пятаки должны показывать примеро одинаковые показания.
Если звонится в 0 - значит, вероятно, шар имеет КЗ с землей.
Если не звонится - значит, вероятно, шар не сел. Нужно еще раз подогреть плату, подуть феном, качнуть камень.

### Прошивка

Чтобы запустить плату, ее необходимо прошить.
Для удешевления конструкции, на плате JTAG header разведен для подключения Altera USB Blaster.

Соответственно, шить SPI флешку можно несколькими путями:

1) засунуть в программатор и прошить BIN-файл прошивки, ПЗУ для GS и конфигурационную ПЗУ (битстрим с 0 адреса, ПЗУ для GS с адреса 0x100000, конфиг с адреса 0x1F0000)
2) подключить Xilinx Platform USB Cable программатор и прошить с помощью iMPACT (битстрим с 0 адреса, ПЗУ для GS с адреса 0x100000, конфиг с адреса 0x1F0000)
3) взять сторонний опен-сорсный проект openFPGALoader и прошить любым JTAG-совместимым программатором, включая Altera USB Blaster
4) то же самое, но через openOCD
5) то же самое, но через urJTAG

Первые 2 пункта нет смысла описывать.
Остановимся подробнее на пунктах 3 и 4 - openFPGALoader (годится пока только для камней xc6slx16, под xc6slx25 в openFpgaLoader'е есть какие-то косяки, флешку он через jtag-spi мост не видит) и openocd.

#### Прошивка с помощью openFPGALoader

1) Ставим openFPGALoader (в современных linux-дистрибутивах он есть из коробки, но версия древняя, поэтому лучше собрать из сырцов согласно мануалу с сайта производителя). Также имеются сборки под windows и mac os.
2) Подключаем USB Blaster
3) Подаем питание 5В на плату (можно через специальный разъем prg на плате)
4) make flash - прошьет прошивку под камень XC6SLX16 для revA
5) make flash_a1 - прошьет прошивку под камень XC6SLX16 для revA1
6) makr flash_a2 - прошьет прошивку под камень XC6SLX16 для revA2
7) make flash_rom - прольет 32kB образ ПЗУ GS по адресу 0x100000 во флеш (актуально для revA1 и revA2, опционально для revA)
8) make flash_cfg1 - прольет 256 байт конфига cfg1 по адресу 0x1F0000 во флеш (актуально для revA2, опционально для revA и revA1)
9) make flash_cfg2 - прольет 256 байт конфига cfg2 по адресу 0x1F0000 во флеш (актуально для revA2, опционально для revA и revA1)

Если влом юзать makefile, можно руками, пример прошивки битстрима для SLX16:

`openFPGALoader -c usb-blaster --fpga-part xc6slx16ftg256 -f work16/karabas_megabuzz.bit`

Пример прошивки ПЗУ GS:

`openFPGALoader -c usb-blaster --fpga-part xc6slx16ftg256 -f -o 0x100000 ../rtl/sound/gs105b.rom`


После программирования плата запустится.
Покажет индикацию уровня сигнала от максимума до минимума.
Кнопкой RESET также покажет индикацию. Это верный признак, что FPGA-часть платы запустилась.


#### Прошивка с помощью openocd

1) Ставим openocd (в современных linux-дистрибутивах он есть из коробки). Также имеются сборки под windows и mac os.
2) Подключаем USB Blaster
3) Подаем питание 5В на плату (можно через специальный разъем prg на плате)
4) make flash25 - прошьет прошивку под камень XC6SLX25 для revA
5) make flash25_a1 - прошьет прошивку под камень XC6SLX25 для revA1 с ПЗУ GS и конфигом cfg1
6) make flash25_a2 - прошьет прошивку под камень XC6SLX25 для revA2 с ПЗУ GS и конфигом cfg1
7) make flash25_a2_nommc - прошьет прошивку под камень XC6SLX25 для revA2 с ПЗУ GS и конфигом cfg2

Если влом юзать makefile, можно руками, пример прошивки битстрима для SLX25:

`sudo openocd -f ocd.cfg -c "init; jtagspi_init 0 bscan_spi_xc6slx25.bit; jtagspi_program work25/karabas_megabuzz_a2.bin 0; jtagspi_program ../rtl/sound/gs105b.rom 0x100000; jtagspi_program cfg1.rom 0x1F0000; xc6s_program xc6s.tap; exit"`

После программирования плата запустится.
Покажет индикацию уровня сигнала от максимума до минимума.
Кнопкой RESET также покажет индикацию. Это верный признак, что FPGA-часть платы запустилась.

ЗЫ: по аналогии можно шить и XC6SLX16 камни, через openocd. соответствующий jtag-spi мост есть - bscan_spi_xc6slx16.bit.

### DIP-переключатели

На плате revA и revA1 имеется 5 дип-переключателей.
Если его не запаивать, все девайсы на плате будут работать.

#### Назначение переключателей для revA:

1) SW1 - Soundrive + Covox + Beeper
2) SW2 - SAA1099
3) SW3 - General Sound
4) SW4 - TurbosoundFM + Midi
5) SW5 - OPL3

#### Назначение переключателей для revA1:

1) SW1 - DivMMC
2) SW2 - Soundrive + Covox + Beeper + SAA1099
3) SW3 - General Sound
4) SW4 - TurbosoundFM + Midi
5) SW5 - OPL3

#### Назначение переключателей для revA2:

на плате отсутствуют дип-переключатели.
вместо этого в первом байте конфигурационной ПЗУ (cfg1.rom или cfg2.rom) это все переключается битами (1 - включено, 0 - выключено. например байт 0xFF означает, что все оборудование включечно):

- бит 0 - DivMMC
- бит 1 - ZC
- бит 2 - Soundrive + Covox
- бит 3 - Beeper
- бит 4 - SAA1099
- бит 5 - General Sound
- бит 6 - Turbosound + FM + Midi
- бит 7 - OPL3

### Тест-поинты на обратной стороне платы

#### revA

- TP1 - выход /OPL3_RST - для доработки rev.A (сброс OPL3 при старте платы - нужно оторвать ногу 8 RST от платы и завести проводком на TP1)
- TP2 - если засоплить на землю, меняет направление индикаторов
- TP3 - если засоплить на землю, не будет показывать столбик VU
- TP4 - если засоплить на землю, не будет показывать точку VU
- TP5-TP8 - пока свободны
- Остальные - связаны с соотв сигналами конфигурации спартана.

#### revA1

- TP1 - TP4 - зарезервировано
- TP5 - TP8 - системные

#### revA2

- TP1 - TP4 - зарезервировано
- TP5 - TP8 - системные
- I2S - выход звука i2s


